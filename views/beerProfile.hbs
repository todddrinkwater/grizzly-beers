<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Grizzly Beer - {{name}}</title>
  </head>
  <body>
    <header>
      <h1> {{name}} </h1>
      <img src={{imgURL}}>
    </header>
    <h3>Description</h3>
    <p>{{description}}</p>

    <h3>Where to find it:</h3>
    <ul>
      {{#each}}
        <li>{{venueName}}</li>
      {{/each}}
    </ul>

    <h3>Ratings</h3>
    <p>{{rating}} </p><br>

    <p>{{comment}}</p>

  </body>

</html>
